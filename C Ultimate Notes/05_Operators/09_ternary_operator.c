// Conditional or Ternary Operator

// condition ? expr1 : expr2
#include <stdio.h>

int main()
{
    int a = 10, b = 20;
    int max = (a > b) ? a : b;
    printf("Max: %d\n", max);

    return 0;
}