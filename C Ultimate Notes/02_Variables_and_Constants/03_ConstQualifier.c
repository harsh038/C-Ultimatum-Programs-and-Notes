// const Qualifier

// const keyword makes a variable read-only.
#include <stdio.h>

// Example program: Uses const to declare a read-only variable
int main()
{
    const int PI = 3.14;                   // Declares a constant
    printf("The value of PI is %f\n", PI); // Uses the constant
    return 0;
}