// Constants

// Constants are fixed values that do not change during execution.
#include <stdio.h>

// Example program: Defines and uses a constant
int main()
{
    const int MAX = 100;                     // Declares a constant
    printf("The value of MAX is %d\n", MAX); // Uses the constant
    return 0;
}