// Variables

// Variables store data values.
#include <stdio.h>

// Example program: Declares and uses a variable
int main()
{
    int number = 5;                                // Declares an integer variable
    printf("The value of number is %d\n", number); // Uses the variable
    return 0;
}