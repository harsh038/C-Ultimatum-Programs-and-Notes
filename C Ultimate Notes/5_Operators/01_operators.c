// Operators

// Operators perform operations on variables and values.
#include <stdio.h>

int main()
{
    int a = 10, b = 20;
    int sum = a + b; // Addition
    printf("Sum: %d\n", sum);

    return 0;
}