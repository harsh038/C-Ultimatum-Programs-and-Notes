// Decision Making 

// Uses if, if-else, switch to make decisions.
#include <stdio.h>

int main() {
    int a = 10;
    if (a > 5) {
        printf("a is greater than 5\n");
    }
    return 0;
}